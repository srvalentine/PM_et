import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'Vault.com',
  appName: 'Vaul-Tec',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
